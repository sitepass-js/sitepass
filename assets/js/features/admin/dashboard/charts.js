// SitePass STEP88 - admin dashboard charts boundary
(function(){
  'use strict';

  // 88단계는 기존 화면에 없던 차트를 새로 추가하지 않는다.
  // 이후 차트 요구가 확정되면 이 경계 안에서만 구현한다.
  function render(){
    return '';
  }

  window.SitePassAdminDashboardCharts = Object.freeze({
    render: render
  });
})();
